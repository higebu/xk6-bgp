name: ci

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: build, vet, test, e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: go build
        run: go build ./...

      - name: go vet
        run: go vet ./...

      - name: go test
        run: go test -race -count=1 ./...

      - name: install xk6
        run: go install go.k6.io/xk6/cmd/xk6@latest

      - name: xk6 build
        run: xk6 build --with github.com/higebu/xk6-bgp=.

      - name: build fakebgpd
        run: go build -o /tmp/fakebgpd ./cmd/fakebgpd

      - name: smoke ipv4-unicast
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families ipv4-unicast -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            -e COUNT=200 \
            examples/ipv4_unicast.js \
            2>&1 | tee /tmp/k6-ipv4.log
          grep -q 'matched=200' /tmp/k6-ipv4.log

      - name: smoke ipv6-unicast
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen '[::1]:11790' -families ipv6-unicast -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET='[::1]:11790' \
            -e COUNT=200 \
            examples/ipv6_unicast.js \
            2>&1 | tee /tmp/k6-ipv6.log
          grep -q 'matched=200' /tmp/k6-ipv6.log

      - name: smoke mup
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families ipv4-mup -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            examples/mup.js \
            2>&1 | tee /tmp/k6-mup.log
          grep -q 'matched=4' /tmp/k6-mup.log

      - name: smoke evpn
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families l2vpn-evpn -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            examples/evpn.js \
            2>&1 | tee /tmp/k6-evpn.log
          grep -q 'matched=3' /tmp/k6-evpn.log

      - name: smoke srv6-l3vpn
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families l3vpn-ipv4-unicast -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            -e COUNT=200 \
            examples/srv6_l3vpn.js \
            2>&1 | tee /tmp/k6-srv6-l3vpn.log
          grep -q 'matched=200' /tmp/k6-srv6-l3vpn.log

      - name: smoke ipv4-addpath
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families ipv4-unicast -reflect -addpath &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            -e COUNT=100 \
            -e PATHS=2 \
            examples/ipv4_addpath.js \
            2>&1 | tee /tmp/k6-ipv4-addpath.log
          grep -q 'matched=200' /tmp/k6-ipv4-addpath.log

      - name: smoke multi-peer
        run: |
          set -euo pipefail
          /tmp/fakebgpd -listen 127.0.0.1:11790 -families ipv4-unicast -reflect &
          FAKEBGPD_PID=$!
          trap 'kill $FAKEBGPD_PID 2>/dev/null || true' EXIT
          sleep 0.5
          ./k6 run \
            -e TARGET=127.0.0.1:11790 \
            -e NUM_PEERS=2 \
            -e COUNT_PER_PEER=50 \
            examples/multi_peer.js \
            2>&1 | tee /tmp/k6-multi-peer.log
          [ "$(grep -c 'matched=50' /tmp/k6-multi-peer.log)" -eq 2 ]

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false

      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.6.2

  security:
    name: gosec
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - uses: securego/gosec@master
        with:
          args: ./...

  vulnerability:
    name: govulncheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: run govulncheck
        run: govulncheck ./...
